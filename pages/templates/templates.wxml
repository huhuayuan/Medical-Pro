<!-- 医生列表 -->
<template name="doctorList">
    <view class="doctorListInfo" bindtap="doctordetail"  data-doctorId="{{item.doctor_id}}">
      <image class='avatar' src='{{item.image}}'></image>
      <view class="doctorDesc">
        <text class="Dname">{{item.name}}</text>
        <text class='Dhospital'>医院：{{item.hospital}}</text>
        <text class='Ddepartment'>科室：{{item.department}}</text>
        <view>
          <text class='Dnum Dbadge'>医生码</text> {{item.num}}
        </view>
      </view>
      <text class="Dage">{{item.age}}</text>
    </view>
</template>

<!-- 个人中心 -->
<!-- <template name="self">
  <view class="selfItem borderB">
    <text class="selfDesc">1</text>
    <text v-if="{{myInfo.myEarnings}}" style="padding-right: 20rpx">100</text>
    <image class='arrow' src='../../images/arrow.png'></image>
  </view>
</template> -->
